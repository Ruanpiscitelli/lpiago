---
import Layout from '../layouts/Layout.astro';
---

<Layout title="404 - Página não encontrada">
	<main class="error-container">
		<div class="error-content">
			<h1>404</h1>
			<h2>Página não encontrada</h2>
			<p>Desculpe, não conseguimos encontrar a página que você está procurando.</p>
			<a href="/" class="back-button">Voltar para a página inicial</a>
		</div>
		<div class="plane-animation">
			<!-- SVG de um avião animado -->
			<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 100" width="200" height="100">
				<path d="M100,50 L120,40 L140,50 L120,60 Z" fill="#ff0000" class="plane-body" />
				<path d="M120,40 L130,30 L135,40 Z" fill="#ff0000" class="plane-wing" />
				<path d="M120,60 L130,70 L135,60 Z" fill="#ff0000" class="plane-wing" />
			</svg>
		</div>
	</main>
</Layout>

<style>
	.error-container {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		min-height: 100vh;
		background-color: #000000;
		color: #ffffff;
		text-align: center;
		padding: 0 1rem;
	}

	.error-content {
		max-width: 600px;
		margin: 0 auto;
	}

	h1 {
		font-size: 8rem;
		margin: 0;
		line-height: 1;
		color: #ff0000;
		text-shadow: 0 0 10px rgba(255, 0, 0, 0.7);
	}

	h2 {
		font-size: 2rem;
		margin: 0 0 1rem;
	}

	p {
		margin-bottom: 2rem;
		font-size: 1.2rem;
	}

	.back-button {
		display: inline-block;
		background-color: #ff0000;
		color: white;
		padding: 0.8rem 1.5rem;
		text-decoration: none;
		border-radius: 4px;
		font-weight: bold;
		transition: background-color 0.3s ease;
	}

	.back-button:hover {
		background-color: #cc0000;
	}

	.plane-animation {
		margin-top: 3rem;
	}

	.plane-body, .plane-wing {
		filter: drop-shadow(0 0 5px rgba(255, 0, 0, 0.7));
	}

	@keyframes fly {
		0% {
			transform: translateX(-100px) translateY(0) rotate(0);
		}
		25% {
			transform: translateX(-50px) translateY(-20px) rotate(5deg);
		}
		50% {
			transform: translateX(0) translateY(0) rotate(0);
		}
		75% {
			transform: translateX(50px) translateY(20px) rotate(-5deg);
		}
		100% {
			transform: translateX(100px) translateY(0) rotate(0);
		}
	}

	svg {
		animation: fly 5s ease-in-out infinite alternate;
	}

	@media (max-width: 768px) {
		h1 {
			font-size: 6rem;
		}
		h2 {
			font-size: 1.5rem;
		}
	}
</style> 